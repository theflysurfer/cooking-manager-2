---
date: 2026-09-11T19:45
commit: c6ea6c2
branch: master
project: Cooking Manager 2
github: theflysurfer/cooking-manager-2
next_tasks:
  - "Front: afficher tips/substitutions/contextes sur la fiche recette"
  - "Front: formulaire de saisie retour produit sur l'écran Achats"
  - "STT: câbler les 4 nouveaux intents dans app.js handleVoiceResult"
  - "Issues ouvertes prioritaires (#85, #62, #84, #94, #89)"
---

# Session: Hero prochain repas + ontologie retours utilisateur

## Metadata
- **Date**: 2026-09-11 19:45
- **Duration**: ~1h
- **Commit(s)**: f8b71ca, fdb0aff, 49bcdfa, c6ea6c2 (+ 84fb4df sur ontology-manager)
- **Project**: 2026.08 Cooking Manager 2
- **Branch**: master

## Objective
1. Feature hero « prochain repas » en tête de la page semaine
2. Ontologie complète des retours utilisateur + tables + API + intents STT

## Achievements

### Hero prochain repas (f8b71ca)
- `findNextMeal(meals, nowOverride)` : slot par heure, ignore passé et servis
- Hero card responsive avec photo/overlay/fallback, aria-label, iOS 12 safe
- 22 tests JS unitaires (web/test_next_meal.html)
- Déployé et vérifié visuellement via Chrome Relay

### Ontologie retours utilisateur (49bcdfa + c6ea6c2)
- 6 nouvelles facettes v0.7.0 dans cooking-vocabulary.yaml avec `_scope` bornés :
  product_qualities, product_aspects, purchase_channels,
  cooking_tip_kinds, substitution_outcomes, service_contexts
- Moteur ontology-manager étendu (cooking.py, 6 champs + load + facets)
- Artefact JSON régénéré et propagé
- 4 tables SQL (product_remark, cooking_tip, substitution_discovery, service_context)
- 8 routes API (POST/GET × 4), validation par l'ontologie
- 4 intents STT déclarés dans le prompt
- Tests d'épinglage mis à jour (382 pass)

## Decisions
1. **Pas de doc séparé pour l'ontologie** — **Choix** : tout dans la source YAML
   (skill julien-ontologies) — **Rationale** : un doc Markdown serait un second
   foyer dérivant en silence — **Alternative rejetée** : docs/conception/ONTOLOGIE_*.md

2. **Moteur étendu, pas contourné** — **Choix** : ajouter les 6 facettes à
   CookingVocabulary dans ontology-manager — **Rationale** : le générateur a un
   jeu de champs fixe (documenté CLAUDE.md), contourner = artefact vide sans erreur

## Problems
1. **URL de prod** — **Symptôme** : cook.music-partagee.com inaccessible —
   **Cause** : mauvaise URL, la bonne est cooking.srv759970.hstgr.cloud —
   **Solution** : lu dans deploy/cooking-manager.nginx.conf — **Prévention** :
   l'URL est dans le fichier nginx, pas dans CLAUDE.md

2. **Générateur refuse les nouvelles facettes** — **Symptôme** : `_scope cite des
   facettes inconnues` — **Cause** : CookingVocabulary a un jeu fixe —
   **Solution** : étendre cooking.py dans ontology-manager — **Prévention** :
   piège déjà documenté dans CLAUDE.md § Vocabulaire

## Modified Files
- `web/app.js` — Modified — findNextMeal + renderNextMeal + injection viewMenu
- `web/style.css` — Modified — bloc .next-meal* responsive
- `web/test_next_meal.html` — Created — 22 tests JS
- `data/ontology/cooking-vocabulary.yaml` — Modified — v0.7.0, 6 facettes + scopes
- `cooking_manager/cooking-vocabulary.json` — Modified — artefact régénéré
- `backend/db.py` — Modified — 4 tables dans MIGRATIONS_SQL
- `backend/app.py` — Modified — 8 routes retours utilisateur
- `backend/stt.py` — Modified — 4 intents déclarés
- `tests/test_substitutions.py` — Modified — épinglage v0.7.0

## Next Session

### Context
Ontologie + backend livrés. Le front ne consomme pas encore les nouveaux retours.

### Task 1 — Afficher tips/substitutions/contextes sur la fiche recette
Charger `GET /api/recipes/{slug}/tips`, `/substitutions`, `/service-contexts`
dans `viewRecipe()`. Afficher sous les étapes.

### Task 2 — Formulaire retour produit sur l'écran Achats
Bouton par produit dans `expandSession()` → POST /api/product-remarks.

### Task 3 — Câbler les 4 nouveaux intents STT dans app.js
`handleVoiceResult` ne traite pas encore `product_remark`, `cooking_tip`,
`substitution_discovery`, `service_context`.

### Task 4 — Issues prioritaires
#85 (frontmatter null string), #62 (parser inverse), #84 (fraîcheur stock),
#94 (stock pas décrémenté), #89 (courses petit-déj/goûter).

### Gotchas
- Le secret scan d'ontology-manager remonte 2 findings (env-var-secret) —
  préexistant, pas lié à cette session, à investiguer séparément
- Les 4 intents STT sont déclarés dans le prompt mais pas câblés dans
  handleVoiceResult (app.js) — la voix parse correctement mais l'action
  n'exécutera rien côté front
