---
date: 2026-08-10T23:43
commit: pending
branch: master
project: 2026.08 Cooking Manager 2
github: theflysurfer/cooking-manager-2
next_tasks:
  - "Valider le endpoint /api/menus/2026-08-10_semaine-begles/compatibility avec les données tablée"
  - "[hérité] Corriger le parsing des ingrédients avec sous-sections ### (refs #58)"
  - "[hérité] UX d'édition de menu (refs #33)"
  - "[hérité] Phase 3 : migrer Waaker SQLite → PostgreSQL (refs #51)"
  - "[hérité] Intégrer des recettes Instagram (refs #52)"
  - "[hérité] TTS retour pour tous les intents (refs #45)"
  - "[hérité] Valider le front sur l'iPad mini 2 réel"
---

# Session : Extension du menu Bègles + déploiement tablée

## Metadata
- **Date** : 2026-08-10 23:43
- **Duration** : ~30 min
- **Commit(s)** : pending (aucun changement de code — session opérationnelle)
- **Project** : 2026.08 Cooking Manager 2
- **Branch** : master

## Objective

Déployer le code tablée (commit `58ab8d6`), seeder les données familiales,
et compléter le menu de la semaine Bègles (dim 10 → mer 13) avec jeudi 14
et vendredi 15.

## Achievements

- **Déploiement VPS** — code `58ab8d6` déployé sur srv759970 (`git pull` +
  `pip install` + `systemctl restart`), redémarrage vérifié via
  `ExecMainStartTimestamp`
- **Seed des données tablée** — `POST /api/seed` : 4 personnes, 1 foyer,
  6 relations, 2 schedules de garde, 6 schedules de cantine
- **Ingestion complète** — `POST /api/ingest` : 57 recettes, 3 menus,
  14 convives, 14 personnes (dual-write), 256 items garde-manger,
  37 photos liées, 0 orphelins
- **Extension du menu Bègles** — 4 repas ajoutés (jeu 14 + ven 15) :
  - Jeu midi : Assiette froide (crevettes, houmous, crudités)
  - Jeu soir : Curry coco lieu noir + lentilles corail
  - Ven midi : Tortilla-pizza atelier
  - Ven soir : Saumon grillé + lentilles vertes + poêlée méditerranéenne
- **Vault synchronisé** — fichier menu poussé via `rclone copy` vers
  Dropbox cloud, propagé au mount VPS, ré-ingestion OK (28 repas liés,
  0 orphelins)

## Decisions

1. **Choix des 4 recettes**
   - **Choix** : variété par rapport aux 4 jours précédents, tout
     pescétarien (contrainte Clémence), aucun conflit d'aversion
   - **Rationale** : froid le jeudi midi (été), one-pot le soir, atelier
     participatif le vendredi (vacances), méditerranéen-oméga-3 en clôture
   - **Alternative rejetée** : mafé au poisson (déjà un mafé mardi)

## Modified Files

- `Noyau/Cuisine/Menus/2026-08-10_semaine-begles.md` (vault Obsidian) —
  Modified — extension dim 10–mer 13 → dim 10–ven 15, 4 repas ajoutés,
  alertes compatibilité complétées

## Next Session

### Context

Le code tablée est déployé et les données seedées. Le menu Bègles couvre
désormais dim 10 → ven 15. Prochaine étape logique : valider que la
compatibilité data-driven (`load_household_config`) produit le même
résultat que l'ancienne version à constantes en dur.

### Task 1 — Valider la compatibilité data-driven

Tester `/api/menus/2026-08-10_semaine-begles/compatibility`. Comparer le
résultat avec les alertes manuelles du menu (Léa/olives, Clémence/viande,
Béatrice/épices). Les deux doivent concorder.

### Task 2 — [hérité] Corriger le parsing des ingrédients (refs #58)

Certaines recettes avec sous-sections `###` ont leurs ingrédients vides en
DB. Le parser s'arrête à la première sous-section au lieu de les traverser.

### Task 3 — [hérité] UX d'édition de menu (refs #33)

Instanciation autonome sans Claude Code — créer/modifier un menu depuis le
front.

### Task 4 — [hérité] Migrer Waaker SQLite → PostgreSQL (refs #51)

### Task 5 — [hérité] Intégrer des recettes Instagram (refs #52)

### Task 6 — [hérité] TTS retour pour tous les intents (refs #45)

### Task 7 — [hérité] Valider le front sur l'iPad mini 2 réel

### Gotchas

- `POST /api/seed` est idempotent (`ON CONFLICT DO NOTHING`) — relancer
  sans risque
- Le menu vault est la source de vérité pour `meals:` ; le corps markdown
  (grille) est informatif
- Après un `rclone copy` vers Dropbox, compter ~15-30 s de propagation
  vers le mount VPS avant de relancer l'ingest
- Deux sources de vérité coexistent (`convive` et `person`) — la migration
  destructive de `convive` est reportée
