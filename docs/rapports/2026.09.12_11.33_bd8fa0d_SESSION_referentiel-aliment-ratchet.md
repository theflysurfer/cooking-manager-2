---
date: 2026-09-12T11:33
commit: bd8fa0d
branch: master
project: 2026.08 Cooking Manager 2
github: theflysurfer/cooking-manager-2
next_tasks:
  - "#69 lot 3 : 17 fiches vault à synchroniser sur le VPS (rclone) puis POST /api/food/import"
  - "Brainstorming polenta section 4 — naissance d'une aversion depuis les retours de table"
  - "Trois axes d'ontologie restants : food_groups · food_contains · glycemic_bands"
  - "[hérité] #73 #76 #77 #78 #82 #85 #86 #88 #89 #93 #94 #95"
---

# Session : référentiel aliment complet et ratchet

## Metadata
- **Date** : 2026-09-12 11:33
- **Commits** : `faa51da`, `bd8fa0d`
- **Issues** : #69 avancée (lot 2 terminé)
- **Handoff précédent** : [la nature d'un produit et les kilojoules pris pour des calories](2026.09.08_15.52_43a8b5c_SESSION_la-nature-d-un-produit-et-les-kilojoules-pris-pour-des-calories.md)

## Objectif

Exécuter le handoff : valider l'iPad, puis rattacher les 51 produits `single`
restants sans `food_key`. Ajouter une gate de non-régression inspirée des
skills TDD et constraint-driven.

## Ce qui a été fait

### 1. iPad validé par Julien

iOS 12 audit : 100/100. VPS health ok. Julien a testé l'iPad réel sur les
4 créneaux — confirmé fonctionnel.

### 2. 51 produits `single` rattachés — 0 reste

Trois passes :
- **22 liens directs** vers des foods existants (oeuf, mangue, chapelure,
  parmesan, pois chiche boite, sardine huile, etc.)
- **5 liens supplémentaires** (fromage-blanc→fromage blanc, fusilli→tagliatelle,
  thon→thon blanc, digestive→petit-beurre)
- **17 aliments créés** en DB + fiches vault + `gen_staple_fiches.py` :
  betterave, épinard, noix, lentille verte, lentille corail, orzo, figue,
  mirabelle, sole, galette riz, algue nori, fromage brebis, pain khorasan,
  skyr, yaourt grec, artichaut, salsifis
- **24 derniers liens** vers les nouveaux aliments

Compteurs finaux : **183 food** (était 166), **91/91 single linked** (était 40/91).

### 3. Analyse des skills TDD et constraint-driven

Julien a demandé comment construire une gate de non-régression. Lecture et
synthèse des skills `test-driven-development` (Prove-It pattern, Beyoncé Rule,
DAMP > DRY, state not interactions) et `constraint-driven-development` (ratchet,
floor, quatre questions). Recommandation appliquée : tests pytest (dans les
gates existantes) + section ratchet dans CLAUDE.md.

### 4. Tests de non-régression écrits

Deux tests dans `TestLinkingRatchet` :
- `test_all_singles_with_a_matching_food_are_linked` — N produits single +
  food existant → tous liés
- `test_mixed_natures_only_singles_are_linked` — single+composite mélangés →
  seuls les singles sont liés, composites jamais

Section « Contraintes mesurées (ratchet) » ajoutée au CLAUDE.md : 4 métriques,
toutes vérifiées par pytest, bloquantes au ship.

## Décisions

1. **Les formes CIQUAL des 17 aliments sont non ambiguës.** Betterave/artichaut/
   salsifis en « cuit/appertisé », lentilles en « sèche », sole/fruits en
   « cru/fraîche », nori en « séchée ». Aucune n'a requis un arbitrage humain
   sur cru vs cuit — ce sont des aliments à forme unique ou évidente.
2. **Le ratchet vit dans CLAUDE.md, pas dans un CONSTRAINTS.md séparé.** Le projet
   est petit, une section suffit.
3. **Les fiches vault sont la source reproductible ; la DB est la source de
   vérité en fonctionnement.** Les deux doivent converger, mais le prochain
   `POST /api/food/import` (après rclone sync) est le geste qui les aligne.

## Fichiers touchés

- `scripts/gen_staple_fiches.py` — +17 aliments (16 insertions)
- `tests/test_food_import.py` — +2 tests ratchet `TestLinkingRatchet`
- `CLAUDE.md` — section « Contraintes mesurées (ratchet) »
- **Vault Obsidian** : 17 fiches `generiques/*.md` (betterave, épinard, noix…)
- **Base** : 17 `food` créés, 51 `product.food_key` mis à jour

## Next Session

### Context

Le référentiel tient : 0 single non lié, tests en place. Les fiches vault sont
sur Dropbox mais pas encore synchronisées sur le VPS (rclone). Le prochain
ingest confirmera la convergence vault↔DB.

### État VPS

`cooking-manager` actif, `43a8b5c` (pas encore les commits de cette session).
Base : 183 food, 170 product (91 single/79 composite), 91 linked, 0 composite lié.

### Task 1 — Synchroniser les fiches vault et relancer l'ingest

Les 17 fiches sont sur Dropbox. Attendre le rclone sync ou forcer un
`rclone copy`. Puis :
```bash
ssh srv759970 'cd /opt/cooking-manager-2 && git pull && .venv/bin/pip install -q . && sudo systemctl restart cooking-manager'
ssh srv759970 'curl -s -X POST localhost:8795/api/food/import?dry_run=true'
```
L'import doit trouver les 17 fiches et ne rien changer en DB (déjà à jour).

### Task 2 — Brainstorming polenta section 4

Sections 1 à 3 approuvées. Reste : comment une aversion naît d'un
`recipe_feedback` et remonte en `dietary_preference`. Puis spec et
writing-plans.

### Task 3 — Axes d'ontologie

`food_groups` · `food_contains` · `glycemic_bands` — à poser dans
`cooking-vocabulary.yaml`.

### Gotchas

- Les fiches vault ne sont PAS encore sur le VPS (rclone sync en attente).
  Ne pas lancer l'import avant que le mount soit à jour.
- Le VPS est encore sur `43a8b5c` — faire `git pull` avant tout test.
- `POST /api/food/import` lit le vault via rclone mount : timeout possible
  si le mount est lent (vécu cette session).
